<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="polinema.ac.id.dtschapter03_starter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_success"></activity>
        <activity android:name=".activity_forgot_password" />
        <activity android:name=".activity_reset_password" />
        <activity android:name=".activity_slide_assign" />
        <activity android:name=".activity_welcome_slide_superhero" />
        <activity android:name=".activity_welcome_back" />
        <activity android:name=".WelcomeSlideCalendar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>